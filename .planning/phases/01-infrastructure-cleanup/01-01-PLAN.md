---
phase: 01-infrastructure-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/GameConfig.js
  - src/systems/GameRegistry.js
  - src/scenes/GameScene.js        # deleted
  - src/scenes/Level1Scene.js      # deleted
  - src/sprites/Player.js          # deleted
  - src/sprites/Enemy.js           # deleted
  - src/sprites/Boss.js            # deleted
  - src/sprites/Coin.js            # deleted
  - src/sprites/Book.js            # deleted
  - src/data/level1Data.js         # deleted
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-04
  - SCORE-04

must_haves:
  truths:
    - "No v1 platformer files exist in src/ — the directory contains only v2 infrastructure files"
    - "npm run build completes with exit code 0 after removals"
    - "GameRegistry exports PLAYER_NAME, SCORE_L1, SCORE_L2, SCORE_L3, SCORE_L4, SCORE_L5 keys"
    - "initRegistry seeds PLAYER_NAME to empty string and all score keys to 0"
  artifacts:
    - path: "src/systems/GameRegistry.js"
      provides: "v2 registry keys: PLAYER_NAME + SCORE_L1..L5 alongside v1 stat keys"
      exports: ["KEYS", "initRegistry"]
      contains: "PLAYER_NAME"
    - path: "src/config/GameConfig.js"
      provides: "Scene array with no Level1Scene reference"
      contains: "scene: [BootScene, TitleScene, HUDScene]"
  key_links:
    - from: "src/config/GameConfig.js"
      to: "src/scenes/Level1Scene.js"
      via: "import — must be REMOVED"
      pattern: "Level1Scene"
    - from: "src/systems/GameRegistry.js"
      to: "initRegistry() call in BootScene"
      via: "PLAYER_NAME and SCORE_L1..L5 must be set on first boot"
      pattern: "PLAYER_NAME"
---

<objective>
Remove all v1 platformer-specific source files and extend GameRegistry with the v2 keys needed by subsequent phases.

Purpose: Clear the debris left by v1 so the codebase compiles cleanly with only v2 infrastructure. Add PLAYER_NAME and per-level score keys now so TitleScene (Plan 02) and mini-game scenes (Phases 3-7) can reference stable key constants.

Output: Clean src/ tree (no Level1Scene/Player/Enemy/Boss/Coin/Book/level1Data), updated GameConfig.js scene array, extended GameRegistry with v2 keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/augustin/Claude/video-game/.planning/PROJECT.md
@/Users/augustin/Claude/video-game/.planning/ROADMAP.md
@/Users/augustin/Claude/video-game/.planning/phases/01-infrastructure-cleanup/01-CONTEXT.md

<interfaces>
<!-- Current GameConfig.js scene array (to be modified) -->
From src/config/GameConfig.js:
```javascript
import { Level1Scene } from '../scenes/Level1Scene.js'   // DELETE THIS LINE
// scene array currently:
scene: [BootScene, TitleScene, Level1Scene, HUDScene]    // remove Level1Scene entry
```

<!-- Current GameRegistry.js KEYS object (to be extended) -->
From src/systems/GameRegistry.js:
```javascript
export const KEYS = Object.freeze({
  // v1 Player state — KEEP
  HEALTH: 'health', HEALTH_MAX: 'healthMax', COINS: 'coins',
  CHECKPOINT_X: 'checkpointX', CHECKPOINT_Y: 'checkpointY',
  // v1 Level progress — KEEP
  COINS_COLLECTED: 'coinsCollected', BOOK_COLLECTED: 'bookCollected', BOSS_DEFEATED: 'bossDefeated',
  // v1 Stats — KEEP
  STAT_SALES: 'statSales', STAT_TECH: 'statTech', STAT_GRIT: 'statGrit',
  STAT_EQ: 'statEQ', STAT_LANGUAGES: 'statLanguages',
  STAT_INDEPENDENCE: 'statIndependence', STAT_TEAMPLAYER: 'statTeamPlayer',
  // v2 NEW — ADD THESE:
  PLAYER_NAME: 'playerName',
  SCORE_L1: 'scoreL1', SCORE_L2: 'scoreL2', SCORE_L3: 'scoreL3',
  SCORE_L4: 'scoreL4', SCORE_L5: 'scoreL5',
})

export function initRegistry(scene) {
  // ... existing v1 seeds ...
  // v2 NEW — ADD THESE seeds:
  reg.set(KEYS.PLAYER_NAME, '')
  reg.set(KEYS.SCORE_L1, 0)
  reg.set(KEYS.SCORE_L2, 0)
  reg.set(KEYS.SCORE_L3, 0)
  reg.set(KEYS.SCORE_L4, 0)
  reg.set(KEYS.SCORE_L5, 0)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete v1 platformer files and clean GameConfig imports</name>
  <read_first>
    - /Users/augustin/Claude/video-game/src/config/GameConfig.js
    - /Users/augustin/Claude/video-game/src/scenes/Level1Scene.js (confirm exists before delete)
    - /Users/augustin/Claude/video-game/src/scenes/GameScene.js (confirm exists before delete)
  </read_first>
  <files>
    src/scenes/Level1Scene.js,
    src/scenes/GameScene.js,
    src/sprites/Player.js,
    src/sprites/Enemy.js,
    src/sprites/Boss.js,
    src/sprites/Coin.js,
    src/sprites/Book.js,
    src/data/level1Data.js,
    src/config/GameConfig.js
  </files>
  <action>
    Step 1 — Delete these eight files (they are v1 platformer debris, no longer referenced after this plan):
      - src/scenes/Level1Scene.js
      - src/scenes/GameScene.js
      - src/sprites/Player.js
      - src/sprites/Enemy.js
      - src/sprites/Boss.js
      - src/sprites/Coin.js
      - src/sprites/Book.js
      - src/data/level1Data.js

    Step 2 — Rewrite src/config/GameConfig.js. Remove the Level1Scene import and remove Level1Scene from the scene array. The final scene array must be [BootScene, TitleScene, HUDScene]. Leave all other config unchanged (type, width, height, parent, backgroundColor, physics, pixelArt, scale).

    The new GameConfig.js must look like:
    ```javascript
    import * as Phaser from 'phaser'
    import { BootScene } from '../scenes/BootScene.js'
    import { TitleScene } from '../scenes/TitleScene.js'
    import { HUDScene } from '../scenes/HUDScene.js'

    export const GameConfig = {
      type: Phaser.AUTO,
      width: 1280,
      height: 720,
      parent: 'game',
      backgroundColor: '#1a1a2e',
      physics: {
        default: 'arcade',
        arcade: {
          gravity: { y: 800 },
          debug: false,
        },
      },
      scene: [BootScene, TitleScene, HUDScene],
      pixelArt: true,
      scale: {
        mode: Phaser.Scale.FIT,
        autoCenter: Phaser.Scale.CENTER_BOTH,
      },
    }
    ```

    Do NOT add LevelSelectHub here — that is Plan 02's responsibility.
  </action>
  <verify>
    <automated>
      cd /Users/augustin/Claude/video-game && npm run build 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "Level1Scene" /Users/augustin/Claude/video-game/src/` returns no matches
    - `grep -r "GameScene" /Users/augustin/Claude/video-game/src/` returns no matches
    - `ls /Users/augustin/Claude/video-game/src/sprites/` returns empty or directory not found
    - `ls /Users/augustin/Claude/video-game/src/data/` returns empty or directory not found
    - `grep "scene:" /Users/augustin/Claude/video-game/src/config/GameConfig.js` shows `[BootScene, TitleScene, HUDScene]`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All eight v1 files deleted. GameConfig.js scene array is [BootScene, TitleScene, HUDScene]. Build passes with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Extend GameRegistry with PLAYER_NAME and per-level score keys</name>
  <read_first>
    - /Users/augustin/Claude/video-game/src/systems/GameRegistry.js
  </read_first>
  <files>src/systems/GameRegistry.js</files>
  <action>
    Edit src/systems/GameRegistry.js to add v2 keys to the KEYS object and seed them in initRegistry.

    In the KEYS object, add after the last v1 stat key (STAT_TEAMPLAYER):
    ```javascript
    // v2 — player identity
    PLAYER_NAME: 'playerName',

    // v2 — per-level scores (0–100)
    SCORE_L1: 'scoreL1',
    SCORE_L2: 'scoreL2',
    SCORE_L3: 'scoreL3',
    SCORE_L4: 'scoreL4',
    SCORE_L5: 'scoreL5',
    ```

    In initRegistry(), add after the last existing reg.set() call:
    ```javascript
    reg.set(KEYS.PLAYER_NAME, '')
    reg.set(KEYS.SCORE_L1,    0)
    reg.set(KEYS.SCORE_L2,    0)
    reg.set(KEYS.SCORE_L3,    0)
    reg.set(KEYS.SCORE_L4,    0)
    reg.set(KEYS.SCORE_L5,    0)
    ```

    Keep all existing v1 KEYS entries and reg.set() calls untouched. The KEYS object must remain frozen (Object.freeze call stays).
  </action>
  <verify>
    <automated>
      node --input-type=module <<'EOF'
import { KEYS, initRegistry } from '/Users/augustin/Claude/video-game/src/systems/GameRegistry.js'
const required = ['PLAYER_NAME','SCORE_L1','SCORE_L2','SCORE_L3','SCORE_L4','SCORE_L5']
const missing = required.filter(k => !(k in KEYS))
if (missing.length > 0) { console.error('MISSING:', missing); process.exit(1) }
console.log('OK — all v2 keys present:', required.map(k => KEYS[k]).join(', '))
EOF
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "PLAYER_NAME" /Users/augustin/Claude/video-game/src/systems/GameRegistry.js` returns a match
    - `grep "SCORE_L1" /Users/augustin/Claude/video-game/src/systems/GameRegistry.js` returns a match
    - `grep "SCORE_L5" /Users/augustin/Claude/video-game/src/systems/GameRegistry.js` returns a match
    - `grep "Object.freeze" /Users/augustin/Claude/video-game/src/systems/GameRegistry.js` still returns a match (freeze not removed)
    - `grep "playerName" /Users/augustin/Claude/video-game/src/systems/GameRegistry.js` returns a match (string value present)
    - Node verify script above exits 0
  </acceptance_criteria>
  <done>KEYS contains PLAYER_NAME and SCORE_L1..L5 with string values. initRegistry seeds them to '' and 0 respectively. All v1 keys untouched.</done>
</task>

</tasks>

<verification>
After both tasks:

```bash
# No v1 files remain
grep -r "Level1Scene\|GameScene\|Level1Data\|Enemy\|Boss\|Coin\|Book" /Users/augustin/Claude/video-game/src/ --include="*.js"

# GameRegistry has v2 keys
grep -E "PLAYER_NAME|SCORE_L1|SCORE_L5" /Users/augustin/Claude/video-game/src/systems/GameRegistry.js

# Build passes
cd /Users/augustin/Claude/video-game && npm run build
```
</verification>

<success_criteria>
- Zero v1 platformer files in src/
- npm run build exits 0
- GameRegistry KEYS includes PLAYER_NAME, SCORE_L1, SCORE_L2, SCORE_L3, SCORE_L4, SCORE_L5
- initRegistry seeds those keys with '' and 0
- GameConfig.js scene array is exactly [BootScene, TitleScene, HUDScene]
</success_criteria>

<output>
After completion, create `/Users/augustin/Claude/video-game/.planning/phases/01-infrastructure-cleanup/01-01-SUMMARY.md`
</output>
