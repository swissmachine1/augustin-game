---
phase: 02-player-controller-animations
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/sprites/Player.js
  - src/scenes/GameScene.js
autonomous: false
requirements:
  - ANIM-01
  - ANIM-02
  - ANIM-03

must_haves:
  truths:
    - "Player rectangle changes color to reflect idle, run, jump, and fall states"
    - "Animation state transitions are immediate and in sync with movement input"
    - "Placeholder colors can be replaced with real sprites by changing one method — no logic rewrite needed"
    - "player.update(delta) is called every frame from GameScene.update()"
    - "A human playing the game can see all four visual states by moving and jumping"
  artifacts:
    - path: "src/sprites/Player.js"
      provides: "Animation state machine with IDLE/RUN/JUMP/FALL states driving rectangle color"
      contains: "_updateAnimState\|ANIM_STATE\|_currentState"
    - path: "src/scenes/GameScene.js"
      provides: "GameScene.update() calls player.update(delta)"
      contains: "player.update"
  key_links:
    - from: "src/scenes/GameScene.js update()"
      to: "Player.update(delta)"
      via: "this.player.update(this.sys.game.loop.delta) or scene delta"
      pattern: "player\\.update"
    - from: "Player._updateAnimState()"
      to: "sprite.setFillStyle()"
      via: "color change per state"
      pattern: "setFillStyle"
---

<objective>
Add an animation state machine to Player.js that drives rectangle color changes per movement state, then wire player.update(delta) into GameScene.update() so the full controller runs every frame.

Purpose: Visual state feedback is how a playtester verifies the controller is working correctly. It also establishes the state machine architecture that will be swapped for real sprites later — without touching any game logic.

Output: Updated Player.js with ANIM_STATE enum and _updateAnimState() driving setFillStyle() per state. Updated GameScene.js calling player.update() each frame. Human verification checkpoint to confirm all four states are visible.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/augustin/Claude/video-game/.planning/ROADMAP.md
@/Users/augustin/Claude/video-game/.planning/STATE.md
@/Users/augustin/Claude/video-game/src/sprites/Player.js
@/Users/augustin/Claude/video-game/src/scenes/GameScene.js

<interfaces>
From src/sprites/Player.js after Plans 01+02 (read before editing):

  class Player:
    constructor(scene, x, y) -- sprite (rectangle, 0x00ff88), body, keys, physics state vars
    update(delta)            -- calls _handleHorizontal(), _handleJump(delta), _applyAsymmetricGravity()
    _handleHorizontal()      -- acceleration-based movement
    _handleJump(delta)       -- full jump system
    _applyAsymmetricGravity() -- asymmetric gravity
    get x(), get y()         -- position getters
    destroy()                -- sprite.destroy()

From src/scenes/GameScene.js after Phase 1 (read before editing):

  update() {
    this.handleDeath()
    // player.update will be added here
  }

Phaser 4 rectangle API:
  sprite.setFillStyle(color)   -- change rectangle fill color (hex integer)
  sprite.setSize(w, h)         -- change rectangle dimensions (optional for states)

Phaser scene delta:
  scene.sys.game.loop.delta    -- frame delta in ms (alternative: use update(time, delta) signature)
  Note: Phaser Scene.update(time, delta) receives delta as second parameter automatically
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add animation state machine to Player.js</name>
  <files>src/sprites/Player.js</files>
  <read_first>
    - src/sprites/Player.js — read the FULL file as updated by Plans 01+02 before editing
  </read_first>
  <behavior>
    - ANIM_STATE const object at top of file (after PLAYER_CONSTANTS): { IDLE: 'idle', RUN: 'run', JUMP: 'jump', FALL: 'fall' }
    - ANIM_COLORS const object mapping each state to a hex color: IDLE: 0x00ff88 (original green), RUN: 0x00ccff (cyan), JUMP: 0xffff00 (yellow), FALL: 0xff8800 (orange)
    - In constructor: this._currentState = ANIM_STATE.IDLE
    - update(delta) calls this._updateAnimState() AFTER all other methods
    - _updateAnimState() logic:
        onGround = this.body.blocked.down
        moving = Math.abs(this.body.velocity.x) > 10
        rising = this.body.velocity.y < -50
        falling = this.body.velocity.y > 50
        If NOT onGround and rising: newState = JUMP
        Else if NOT onGround and falling: newState = FALL
        Else if onGround and moving: newState = RUN
        Else: newState = IDLE
        If newState !== _currentState: _currentState = newState; sprite.setFillStyle(ANIM_COLORS[newState])
    - setFillStyle only called on STATE CHANGE (not every frame) — avoids redundant draw calls
    - Comment in _updateAnimState: "Swap setFillStyle() for sprite.setTexture() / sprite.play() when real sprites are ready"
  </behavior>
  <action>
Add animation state machine to src/sprites/Player.js. Read the full file first — preserve all Plans 01+02 work.

Add at top of file (after PLAYER_CONSTANTS, before class):

  const ANIM_STATE = Object.freeze({
    IDLE: 'idle',
    RUN:  'run',
    JUMP: 'jump',
    FALL: 'fall',
  })

  const ANIM_COLORS = Object.freeze({
    [ANIM_STATE.IDLE]: 0x00ff88,  // green — standing still
    [ANIM_STATE.RUN]:  0x00ccff,  // cyan — running
    [ANIM_STATE.JUMP]: 0xffff00,  // yellow — airborne (rising)
    [ANIM_STATE.FALL]: 0xff8800,  // orange — airborne (falling)
  })

In constructor, after all existing setup:
  this._currentState = ANIM_STATE.IDLE

In update(delta), as the LAST call after all other methods:
  this._updateAnimState()

Add _updateAnimState() method:
  const onGround = this.body.blocked.down
  const moving   = Math.abs(this.body.velocity.x) > 10
  const rising   = this.body.velocity.y < -50
  const falling  = this.body.velocity.y > 50

  let newState
  if (!onGround && rising)       newState = ANIM_STATE.JUMP
  else if (!onGround && falling) newState = ANIM_STATE.FALL
  else if (onGround && moving)   newState = ANIM_STATE.RUN
  else                           newState = ANIM_STATE.IDLE

  if (newState !== this._currentState) {
    this._currentState = newState
    // Swap setFillStyle() for sprite.setTexture() / sprite.play() when real sprites are ready (ANIM-02)
    this.sprite.setFillStyle(ANIM_COLORS[newState])
  }
  </action>
  <verify>
    <automated>grep -n "ANIM_STATE\|ANIM_COLORS\|_currentState\|_updateAnimState\|setFillStyle" /Users/augustin/Claude/video-game/src/sprites/Player.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "ANIM_STATE" src/sprites/Player.js returns matches for definition and all 4 state values (IDLE, RUN, JUMP, FALL)
    - grep "ANIM_COLORS" src/sprites/Player.js returns matches for definition and all 4 color entries
    - grep "_updateAnimState" src/sprites/Player.js returns at least 2 matches (definition + call in update)
    - grep "_currentState" src/sprites/Player.js returns matches for init in constructor and assignment in _updateAnimState
    - grep "setFillStyle" src/sprites/Player.js returns exactly 1 match (inside _updateAnimState — only called on state change)
    - grep "Object.freeze" src/sprites/Player.js returns at least 3 matches (PLAYER_CONSTANTS, ANIM_STATE, ANIM_COLORS)
    - All Plans 01+02 patterns still present: grep "setAccelerationX\|_handleJump\|_coyoteTimeLeft\|setGravityY" returns matches
  </acceptance_criteria>
  <done>Player.js has ANIM_STATE enum, ANIM_COLORS map, _currentState tracking, and _updateAnimState() that drives setFillStyle() on state change only. Comment in code marks the swap point for real sprites.</done>
</task>

<task type="auto">
  <name>Task 2: Wire player.update(delta) into GameScene.update()</name>
  <files>src/scenes/GameScene.js</files>
  <read_first>
    - src/scenes/GameScene.js — full current implementation (preserve SHUTDOWN handler, death floor, respawn, scene label)
    - src/sprites/Player.js — confirm update(delta) method signature exists
  </read_first>
  <action>
Update GameScene.update() to pass delta to player.update(). Phaser Scene.update() receives (time, delta) as arguments — use the delta parameter.

Change the update method signature and add player.update call:

  update(time, delta) {
    if (this.player) this.player.update(delta)
    this.handleDeath()
  }

The guard (if this.player) prevents errors if update fires before create() completes.

Do NOT change anything else in GameScene.js — preserve the SHUTDOWN handler, scene label, platforms, colliders, and respawn logic exactly as they are.
  </action>
  <verify>
    <automated>grep -n "player.update\|update(time" /Users/augustin/Claude/video-game/src/scenes/GameScene.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "player.update" src/scenes/GameScene.js returns exactly 1 match
    - grep "update(time, delta)" src/scenes/GameScene.js returns 1 match (method signature)
    - grep "handleDeath" src/scenes/GameScene.js still returns a match (not removed)
    - grep "SHUTDOWN" src/scenes/GameScene.js still returns a match (not removed)
    - grep "respawn\|body.reset" src/scenes/GameScene.js still returns a match (not removed)
  </acceptance_criteria>
  <done>GameScene.update(time, delta) calls this.player.update(delta) before handleDeath(). All existing GameScene behavior preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all four animation states in browser</name>
  <what-built>Complete player controller: smooth horizontal movement, variable jump height, double jump, coyote time, jump buffer, asymmetric gravity, and animation state machine showing four distinct rectangle colors.</what-built>
  <how-to-verify>
    Run: npm run dev (from /Users/augustin/Claude/video-game)
    Open: http://localhost:5173
    Press Space to start from title screen.

    Test each state and expected color:
    1. Stand still — rectangle should be GREEN (idle)
    2. Hold right arrow — rectangle should turn CYAN (run)
    3. Press Space — rectangle should turn YELLOW (jump/rising phase)
    4. Watch peak of jump arc — rectangle turns ORANGE (fall/descending phase)
    5. Release Space mid-jump — jump should cut short noticeably vs holding Space for full height
    6. Walk off platform edge, press Space within ~0.12 seconds — should still jump (coyote time)
    7. Press Space just before landing — should jump immediately on touchdown (jump buffer)
    8. Press Space twice in the air — second jump should fire (double jump, slightly lower arc)
    9. Watch fall speed vs rise speed — falling should feel faster/snappier than rising

    If any state color is wrong, or jump mechanics feel broken, describe the specific failure.
  </how-to-verify>
  <resume-signal>Type "approved" if all states work, or describe what is wrong (e.g. "coyote time not working", "fall state not showing orange")</resume-signal>
</task>

</tasks>

<verification>
Run both grep checks:

  grep -n "ANIM_STATE\|ANIM_COLORS\|_updateAnimState\|setFillStyle" /Users/augustin/Claude/video-game/src/sprites/Player.js
  grep -n "player.update\|update(time" /Users/augustin/Claude/video-game/src/scenes/GameScene.js

Expected: All patterns present. Then launch npm run dev and complete the human-verify checkpoint.
</verification>

<success_criteria>
- Player.js has ANIM_STATE (4 values), ANIM_COLORS (4 entries), _currentState, and _updateAnimState()
- setFillStyle() called only on state change — not every frame
- Comment in _updateAnimState marks sprite swap point
- GameScene.update(time, delta) calls player.update(delta) with guard
- Human verifies: green=idle, cyan=run, yellow=jump, orange=fall — all correct in browser
- Human verifies: variable jump height, coyote time, jump buffer, and double jump all work
</success_criteria>

<output>
After completion, create /Users/augustin/Claude/video-game/.planning/phases/02-player-controller-animations/02-03-SUMMARY.md
</output>
