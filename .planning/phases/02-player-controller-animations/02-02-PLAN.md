---
phase: 02-player-controller-animations
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/sprites/Player.js
autonomous: true
requirements:
  - CTRL-02
  - CTRL-03
  - CTRL-04
  - CTRL-05
  - CTRL-06

must_haves:
  truths:
    - "Tapping Space produces a small hop — releasing Space early cuts the jump short"
    - "Holding Space produces a full height jump"
    - "Walking off a platform and pressing Space within 120ms still jumps"
    - "Pressing Space up to 150ms before landing fires the jump on touchdown"
    - "The player falls noticeably faster than they rise — no floaty arc"
    - "A second Space press in the air triggers a second (weaker) jump"
  artifacts:
    - path: "src/sprites/Player.js"
      provides: "Complete jump system: variable height, double jump, coyote time, jump buffer, asymmetric gravity"
      contains: "_handleJump\|coyoteTimer\|jumpBufferTimer\|_doubleJumpAvailable"
  key_links:
    - from: "src/sprites/Player.js"
      to: "Phaser Arcade physics body"
      via: "body.setVelocityY / body.setGravityY"
      pattern: "setVelocityY|setGravityY"
    - from: "src/sprites/Player.js"
      to: "body.blocked.down"
      via: "ground detection for coyote time and jump reset"
      pattern: "blocked\\.down"
---

<objective>
Add the complete jump system to Player.js: variable jump height, double jump, coyote time (120ms), jump buffer (150ms), and asymmetric gravity (2x on descent).

Purpose: This is the single most important feel mechanic. A recruiter who plays for 10 seconds will know immediately if the jump feels right or wrong. Celeste's jump arc is asymmetric — fast rise, slower apex, snappy fall. Coyote time and jump buffering make the controls forgiving without feeling loose.

Output: Updated src/sprites/Player.js with _handleJump(), coyote time tracking, jump buffer tracking, double jump state, and asymmetric gravity applied in update(). PLAYER_CONSTANTS from Plan 01 already contains all needed values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/augustin/Claude/video-game/.planning/ROADMAP.md
@/Users/augustin/Claude/video-game/.planning/STATE.md
@/Users/augustin/Claude/video-game/src/sprites/Player.js

<interfaces>
From src/sprites/Player.js after Plan 01 (must be read before editing):

  PLAYER_CONSTANTS = {
    ACCEL_X: 1200, DRAG_X: 1000, MAX_VEL_X: 300,
    JUMP_VEL: -600,           -- first jump velocity
    GRAVITY_MULT_FALL: 2.0,   -- descent gravity multiplier
    COYOTE_MS: 120,           -- coyote window
    JUMP_BUFFER_MS: 150,      -- buffer window
    DOUBLE_JUMP_VEL: -480,    -- second jump at 80%
  }

  class Player:
    constructor(scene, x, y) -- sets up sprite, body, drag, maxVelocityX, keys, _facingRight
    update(delta)             -- calls _handleHorizontal()
    _handleHorizontal()       -- acceleration-based left/right movement

Phaser 4 Arcade body API (v4-verified):
  body.blocked.down           -- true when standing on solid surface
  body.velocity.y             -- current vertical velocity (negative = moving up)
  body.setVelocityY(value)    -- set vertical velocity directly for jumps
  body.setGravityY(value)     -- add gravity ON TOP of world gravity (not replace)
  scene.time.now              -- current time in ms for coyote/buffer deltas
  Phaser.Input.Keyboard.JustDown(key) -- true only on the frame key was pressed
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement variable jump height and asymmetric gravity</name>
  <files>src/sprites/Player.js</files>
  <read_first>
    - src/sprites/Player.js — read the FULL file as updated by Plan 01 before editing
  </read_first>
  <behavior>
    - In constructor: _doubleJumpAvailable = true, _coyoteTimeLeft = 0, _jumpBufferTimeLeft = 0, _wasOnGround = false
    - update(delta) calls _handleJump(delta) AFTER _handleHorizontal()
    - _handleJump(delta) logic:
        onGround = body.blocked.down
        If onGround and not _wasOnGround (just landed): consume buffered jump if _jumpBufferTimeLeft > 0
        If onGround: reset _coyoteTimeLeft = COYOTE_MS, reset _doubleJumpAvailable = true
        If NOT onGround and _wasOnGround (just left ground): start coyote countdown
        Decrement _coyoteTimeLeft by delta (floor at 0)
        Decrement _jumpBufferTimeLeft by delta (floor at 0)
        _wasOnGround = onGround
        jumpPressed = Phaser.Input.Keyboard.JustDown(keys.space) OR Phaser.Input.Keyboard.JustDown(keys.up) OR Phaser.Input.Keyboard.JustDown(keys.w)
        If jumpPressed: set _jumpBufferTimeLeft = JUMP_BUFFER_MS
        canJump = onGround OR _coyoteTimeLeft > 0
        If jumpPressed and canJump: _doJump() — first jump
        Else if jumpPressed and _doubleJumpAvailable and NOT canJump: _doDoubleJump()
        Variable height: if Space/Up/W NOT held (isDown false) and body.velocity.y < -200: body.setVelocityY(body.velocity.y * 0.85) each frame — cuts jump short on release
    - _doJump(): body.setVelocityY(JUMP_VEL), _coyoteTimeLeft = 0, _jumpBufferTimeLeft = 0
    - _doDoubleJump(): body.setVelocityY(DOUBLE_JUMP_VEL), _doubleJumpAvailable = false
    - Asymmetric gravity: if body.velocity.y > 0 (falling): apply body.setGravityY(worldGravity * (GRAVITY_MULT_FALL - 1)) — adds EXTRA gravity on top of world gravity to get 2x total; if rising: body.setGravityY(0)
    - World gravity reference: scene.physics.world.gravity.y (reads the value set in GameConfig)
  </behavior>
  <action>
Add jump system to src/sprites/Player.js. Read the file fully first — do not overwrite Plan 01 work.

In constructor, after existing setup, add:
  this._doubleJumpAvailable = true
  this._coyoteTimeLeft = 0
  this._jumpBufferTimeLeft = 0
  this._wasOnGround = false

In update(delta), after this._handleHorizontal(), add:
  this._handleJump(delta)
  this._applyAsymmetricGravity()

Add _handleJump(delta) method:
  const onGround = this.body.blocked.down
  const jumpPressed = Phaser.Input.Keyboard.JustDown(this.keys.space)
                   || Phaser.Input.Keyboard.JustDown(this.keys.up)
                   || Phaser.Input.Keyboard.JustDown(this.keys.w)

  // Buffer: record jump intent
  if (jumpPressed) this._jumpBufferTimeLeft = PLAYER_CONSTANTS.JUMP_BUFFER_MS

  // Coyote time: reset when grounded, count down when airborne
  if (onGround) {
    this._coyoteTimeLeft = PLAYER_CONSTANTS.COYOTE_MS
    this._doubleJumpAvailable = true
    // Consume buffered jump on landing
    if (!this._wasOnGround && this._jumpBufferTimeLeft > 0) {
      this._doJump()
    }
  } else {
    this._coyoteTimeLeft = Math.max(0, this._coyoteTimeLeft - delta)
  }
  this._jumpBufferTimeLeft = Math.max(0, this._jumpBufferTimeLeft - delta)
  this._wasOnGround = onGround

  // Jump execution
  const canJump = onGround || this._coyoteTimeLeft > 0
  if (jumpPressed && canJump) {
    this._doJump()
  } else if (jumpPressed && this._doubleJumpAvailable && !canJump) {
    this._doDoubleJump()
  }

  // Variable height: cut jump on early release
  const jumpHeld = this.keys.space.isDown || this.keys.up.isDown || this.keys.w.isDown
  if (!jumpHeld && this.body.velocity.y < -200) {
    this.body.setVelocityY(this.body.velocity.y * 0.85)
  }

Add _doJump():
  this.body.setVelocityY(PLAYER_CONSTANTS.JUMP_VEL)
  this._coyoteTimeLeft = 0
  this._jumpBufferTimeLeft = 0

Add _doDoubleJump():
  this.body.setVelocityY(PLAYER_CONSTANTS.DOUBLE_JUMP_VEL)
  this._doubleJumpAvailable = false

Add _applyAsymmetricGravity():
  const worldGravity = this.scene.physics.world.gravity.y
  if (this.body.velocity.y > 0) {
    // Falling: add extra gravity so total = worldGravity * GRAVITY_MULT_FALL
    this.body.setGravityY(worldGravity * (PLAYER_CONSTANTS.GRAVITY_MULT_FALL - 1))
  } else {
    this.body.setGravityY(0)
  }

Note: setGravityY adds to world gravity, it does not replace it. So worldGravity * (2.0 - 1) = worldGravity * 1.0 added on top of worldGravity * 1.0 = 2x total. Correct.
  </action>
  <verify>
    <automated>grep -n "_handleJump\|_doJump\|_doDoubleJump\|_applyAsymmetricGravity\|_coyoteTimeLeft\|_jumpBufferTimeLeft\|_doubleJumpAvailable\|setVelocityY\|setGravityY\|blocked\.down\|JustDown" /Users/augustin/Claude/video-game/src/sprites/Player.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "_handleJump" src/sprites/Player.js returns at least 2 matches (definition + call in update)
    - grep "_doJump" src/sprites/Player.js returns at least 2 matches (definition + call)
    - grep "_doDoubleJump" src/sprites/Player.js returns at least 2 matches (definition + call)
    - grep "_applyAsymmetricGravity" src/sprites/Player.js returns at least 2 matches
    - grep "_coyoteTimeLeft" src/sprites/Player.js returns matches for init, decrement, and reset
    - grep "_jumpBufferTimeLeft" src/sprites/Player.js returns matches for init, set on press, and decrement
    - grep "_doubleJumpAvailable" src/sprites/Player.js returns matches for init, reset on ground, and set to false on use
    - grep "setVelocityY" src/sprites/Player.js returns at least 2 matches
    - grep "setGravityY" src/sprites/Player.js returns at least 2 matches
    - grep "blocked.down" src/sprites/Player.js returns a match
    - grep "JustDown" src/sprites/Player.js returns a match
    - Plan 01 work still present: grep "setAccelerationX\|setDragX\|_handleHorizontal" returns matches
  </acceptance_criteria>
  <done>Player.js has a complete jump system: variable height via velocity cut on early release, double jump at 80% strength, 120ms coyote time, 150ms jump buffer, and 2x asymmetric gravity on descent. All Plan 01 behavior preserved.</done>
</task>

</tasks>

<verification>
Run: grep -n "setVelocityY\|setGravityY\|blocked.down\|_coyoteTimeLeft\|_jumpBufferTimeLeft\|_doubleJumpAvailable\|JustDown" /Users/augustin/Claude/video-game/src/sprites/Player.js

Expected: All patterns appear. Count of setVelocityY matches should be at least 2 (_doJump and _doDoubleJump). setGravityY should appear twice (falling branch and else branch).

Also run: grep -n "setAccelerationX\|setDragX\|_handleHorizontal" /Users/augustin/Claude/video-game/src/sprites/Player.js

Expected: Plan 01 horizontal movement code still present — not accidentally overwritten.
</verification>

<success_criteria>
- Variable jump height: Space release while rising cuts velocity by 15% per frame until above -200 threshold
- Coyote time: 120ms window after leaving ground (tracked with _coyoteTimeLeft decremented by delta)
- Jump buffer: 150ms window of queued input (tracked with _jumpBufferTimeLeft, consumed on landing)
- Double jump: second Space press in air fires at DOUBLE_JUMP_VEL (-480) and is not available again until landing
- Asymmetric gravity: setGravityY adds worldGravity * 1.0 extra on descent, 0 on ascent — total fall gravity is 2x
- All PLAYER_CONSTANTS values used — no magic numbers in jump logic
</success_criteria>

<output>
After completion, create /Users/augustin/Claude/video-game/.planning/phases/02-player-controller-animations/02-02-SUMMARY.md
</output>
