---
phase: 02-player-controller-animations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sprites/Player.js
autonomous: true
requirements:
  - CTRL-01
  - CTRL-07

must_haves:
  truths:
    - "Player accelerates smoothly left and right — no instant velocity snap"
    - "Releasing left/right decelerates the player to a stop — no abrupt halt"
    - "Player rectangle flips to face the direction of last movement"
    - "All tuning constants are named and grouped at the top of Player.js"
  artifacts:
    - path: "src/sprites/Player.js"
      provides: "Player class with horizontal movement, input wiring, and physics constants"
      contains: "PLAYER_CONSTANTS"
  key_links:
    - from: "src/sprites/Player.js"
      to: "Phaser Arcade physics body"
      via: "body.setAccelerationX / body.setDragX"
      pattern: "setAccelerationX|setDragX"
---

<objective>
Extend the Player class with keyboard input wiring and smooth horizontal movement using Phaser 4 Arcade Physics acceleration and drag — no direct velocity assignment.

Purpose: Horizontal movement must feel tight and responsive before jump mechanics are layered on. Acceleration + drag gives the "weight" that makes Celeste feel different from floaty student projects.

Output: Updated src/sprites/Player.js with named physics constants, cursor/WASD input, smooth left/right acceleration, drag-based deceleration, and horizontal sprite flipping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/augustin/Claude/video-game/.planning/ROADMAP.md
@/Users/augustin/Claude/video-game/.planning/STATE.md
@/Users/augustin/Claude/video-game/src/sprites/Player.js
@/Users/augustin/Claude/video-game/src/scenes/GameScene.js

<interfaces>
From src/sprites/Player.js (current — must preserve all existing behavior):

  export class Player {
    constructor(scene, x, y)  — creates this.sprite (rectangle), this.body (physics body)
    get x()                   — returns this.sprite.x
    get y()                   — returns this.sprite.y
    destroy()                 — calls this.sprite.destroy()
  }

GameScene wires colliders to this.player.sprite — the sprite property MUST remain intact.

Phaser 4 Arcade Physics body API (v4-verified):
  body.setAccelerationX(value)     — set X acceleration each frame
  body.setDragX(value)             — friction when no input (applied automatically)
  body.setMaxVelocityX(value)      — cap horizontal speed
  body.setCollideWorldBounds(true) — already set in constructor
  sprite.setFlipX(bool)            — flip horizontally (true = facing left)
  scene.input.keyboard.addKeys({}) — returns named key map
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire input and implement smooth horizontal movement</name>
  <files>src/sprites/Player.js</files>
  <read_first>
    - src/sprites/Player.js — full current implementation (preserve all existing behavior)
    - src/scenes/GameScene.js — confirm player.sprite is what colliders are attached to
  </read_first>
  <behavior>
    - PLAYER_CONSTANTS object at top of file with: ACCEL_X (1200), DRAG_X (1000), MAX_VEL_X (300), JUMP_VEL (-600), GRAVITY_MULT_FALL (2.0), COYOTE_MS (120), JUMP_BUFFER_MS (150), DOUBLE_JUMP_VEL (-480)
    - constructor wires this.keys via scene.input.keyboard.addKeys for cursors (LEFT, RIGHT, UP, SPACE) and WASD (A, D, W)
    - update(delta) method calls _handleHorizontal()
    - _handleHorizontal: if right pressed, setAccelerationX(+ACCEL_X) and set _facingRight=true; if left pressed, setAccelerationX(-ACCEL_X) and _facingRight=false; else setAccelerationX(0)
    - setDragX(DRAG_X) called once in constructor — drag handles deceleration automatically
    - setMaxVelocityX(MAX_VEL_X) called once in constructor
    - sprite.setFlipX(!_facingRight) called each frame — false when facing right, true when facing left
    - When idle (no left/right input), do not update _facingRight — keep last direction
    - update(delta) signature accepts delta for Plan 02 time-based jump calculations
    - All other existing behavior preserved: sprite, body, x/y getters, destroy()
  </behavior>
  <action>
Rewrite src/sprites/Player.js. Keep ALL existing behavior (sprite creation, body, x/y getters, destroy). Add PLAYER_CONSTANTS, input setup, horizontal movement, and facing direction.

PLAYER_CONSTANTS at top of file:
  ACCEL_X: 1200        (px/s^2 — acceleration when key held)
  DRAG_X: 1000         (px/s^2 — deceleration when no key)
  MAX_VEL_X: 300       (px/s — horizontal speed cap)
  JUMP_VEL: -600       (px/s — jump initial velocity, negative = upward)
  GRAVITY_MULT_FALL: 2.0  (multiplier on descent — Plan 02)
  COYOTE_MS: 120       (ms — coyote time window — Plan 02)
  JUMP_BUFFER_MS: 150  (ms — jump buffer window — Plan 02)
  DOUBLE_JUMP_VEL: -480 (px/s — second jump at 80% — Plan 02)

In constructor, after existing body setup:
  this.body.setDragX(PLAYER_CONSTANTS.DRAG_X)
  this.body.setMaxVelocityX(PLAYER_CONSTANTS.MAX_VEL_X)
  this.keys = scene.input.keyboard.addKeys with named keys: left, right, up, space, a, d, w
  this._facingRight = true

Add update(delta) method that calls this._handleHorizontal()

Add _handleHorizontal() method:
  goLeft = keys.left.isDown OR keys.a.isDown
  goRight = keys.right.isDown OR keys.d.isDown
  If goRight: setAccelerationX(+ACCEL_X), _facingRight = true
  Else if goLeft: setAccelerationX(-ACCEL_X), _facingRight = false
  Else: setAccelerationX(0) — drag handles deceleration
  Always: sprite.setFlipX(!this._facingRight)

Do NOT add jump logic yet — that is Plan 02.
Do NOT call player.update() from GameScene yet — that is Plan 03.
  </action>
  <verify>
    <automated>grep -n "PLAYER_CONSTANTS\|setAccelerationX\|setDragX\|setMaxVelocityX\|setFlipX\|addKeys\|_handleHorizontal\|update(delta" /Users/augustin/Claude/video-game/src/sprites/Player.js</automated>
  </verify>
  <acceptance_criteria>
    - grep "PLAYER_CONSTANTS" src/sprites/Player.js returns a match with at least 8 keys defined
    - grep "setDragX" src/sprites/Player.js returns a match in the constructor
    - grep "setMaxVelocityX" src/sprites/Player.js returns a match in the constructor
    - grep "setAccelerationX" src/sprites/Player.js returns at least 3 matches (right/left/else branches)
    - grep "setFlipX" src/sprites/Player.js returns a match
    - grep "addKeys" src/sprites/Player.js returns a match
    - grep "_handleHorizontal" src/sprites/Player.js returns at least 2 matches (definition + call)
    - grep "update" src/sprites/Player.js returns a match for update(delta) method
    - grep "destroy" src/sprites/Player.js still returns a match (preserved)
    - grep "get x\|get y" src/sprites/Player.js still returns matches (getters preserved)
  </acceptance_criteria>
  <done>Player.js has PLAYER_CONSTANTS, input wiring, smooth horizontal movement via acceleration/drag, and facing direction flip. No magic numbers in movement logic. Existing interface (sprite, body, getters, destroy) fully preserved.</done>
</task>

</tasks>

<verification>
Run: grep -n "PLAYER_CONSTANTS\|setAccelerationX\|setDragX\|setMaxVelocityX\|setFlipX" /Users/augustin/Claude/video-game/src/sprites/Player.js

Expected: All five patterns appear. If any are missing, the movement system is incomplete.

Also verify GameScene still starts without errors — the player module is imported there, so a syntax error in Player.js will surface immediately on npm run dev.
</verification>

<success_criteria>
- Player.js exports PLAYER_CONSTANTS with 8 named tuning values
- constructor sets drag and maxVelocityX once (not per-frame)
- update(delta) exists and calls _handleHorizontal()
- _handleHorizontal uses acceleration (not velocity) for left/right movement
- sprite.setFlipX drives facing direction
- No magic numbers in movement logic — all values from PLAYER_CONSTANTS
- All pre-existing behavior preserved (sprite, body, x/y getters, destroy)
</success_criteria>

<output>
After completion, create /Users/augustin/Claude/video-game/.planning/phases/02-player-controller-animations/02-01-SUMMARY.md
</output>
